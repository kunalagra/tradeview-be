// const.enum.ts
export enum UserType {
	ADMIN = 'admin',
	INDIVIDUAL = 'individual',
}

export enum Broker {
	ZERODHA = 'ZERODHA',
	GROWW = 'GROWW',
}

export enum Status {
	SUCCESS = 'success',
	ERROR = 'error',
	FAILED = 'failed',
}
